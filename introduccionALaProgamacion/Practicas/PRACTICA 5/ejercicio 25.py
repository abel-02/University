#NO HACER LAS FUNCIONES DE ABAJO, ES SOLO USANDO LA LOGICA

def darPatentes(ruta):

def controlVelocidad(patente):

def reincidente(patente):

def costoAnual():

def enviarMulta(patente, costo):




def controlVehicular(ruta,patente,):
    patentes = darPatentes(ruta)
    for patente in patentes:
        velocidad = controlVelocidad(patente)
        autoReincidente = reincidente(patente)
        costo = costoActual()
        if velocidad > 100:
            if autoReincidente == True:
                costo = costo*2
           enviarMulta(patente, costo)    #return    #el costo es la multa

